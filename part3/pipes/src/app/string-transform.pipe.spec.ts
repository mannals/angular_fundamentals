import { StringTransformPipe } from './string-transform.pipe';

describe('StringTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new StringTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
